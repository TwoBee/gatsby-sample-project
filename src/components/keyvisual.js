import '../index.css'
import {
    container,
    heading,
    navLinks,
    navLinkItem,
    navLinkText,
    logoContainer,
    logoContent,
    fullsite,
    siteWrapper,
    navContainer,
    menu,
    menuDesktop,
    mainmenu,
    footerContent
  } from '../styles/layout.module.css'

const keyvisual = ({ imageSrc, Subline, Headline, Catchline }) => {
    return (
        <div>

        </div>
    )}

export default keyvisual;